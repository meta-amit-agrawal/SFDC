trigger LoanTrigger on Loan__c (before insert) {
    if(Trigger.isBefore){
        if(Trigger.isInsert){
            LoanTriggerHandler.onBeforeInsertUpdateLoanManager(Trigger.new);
        }
    }    
}